<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="DarkTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/about_us</item>
        <item name="colorSecondaryVariant">@color/about_us</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Screen background if dark mode is on -->
    <!-- Theme Style for Night Mode Switch -->
    <style name= "nightModeSwitch" parent = "Theme.AppCompat.Light">
        <item name = "colorControlActivated">@color/night_mode</item>
        <item name = "colorSwitchThumbNormal">#FFC107</item>
        <item name = "android:colorForeground">#99FFFFFF</item>
    </style>

    <!--Theme style for Notifications Switch-->
    <style name= "notificationsSwitch" parent = "Theme.AppCompat.Light">
        <item name = "colorControlActivated">@color/notifications</item>
        <item name = "colorSwitchThumbNormal">#FFC107</item>
        <item name = "android:colorForeground">#99FFFFFF</item>
    </style>
    <!--Theme style for Private Account Switch-->
    <style name= "privateAccountSwitch" parent = "Theme.AppCompat.Light">
        <item name = "colorControlActivated">@color/about_us</item>
        <item name = "colorSwitchThumbNormal">#FFFFFF</item>
        <item name = "android:colorForeground">#99FFFFFF</item>
    </style>
</resources>